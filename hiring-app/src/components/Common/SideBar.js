import * as React from "react";
import DisplayResume from "../Common/DisplayResume";
import "../../components/../styles/SideContent.css";

const SideBar = () => {
  return (
    <div className="design">
    <div>
      <DisplayResume />
    </div>
    </div>
  );
};

export default SideBar;
